Autocomplete everywhere
Back button
Cookie unset but probably not invalidated
- Could increase TTL and raise as issue?
- /api/v1/cards
  - Returns full unmasked PAN
  - Returns CVV (which shouldn't be stored)
JWT contains user ID
PAN not encrypted
- Demonstrate via SQLi?
