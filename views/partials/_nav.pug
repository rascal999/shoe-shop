nav.navbar.navbar-dark.bg-primary.navbar-expand
    script(type='text/javascript', src='/js/jquery-3.6.3.min.js')

    .container
        a.navbar-brand(href='/')
          img(src='/images/RBRD.webp',style='height:45px;padding-right:10px')
          label Shoe Shop
        ul.navbar-nav.mr-auto
            li.nav-item
                a.nav-link(href='/')
                    span Home
            li.nav-item
                a.nav-link(href='/report')
                    span Report
            if user
              li.nav-item
                  a.nav-link(href='/billing')
                      span Billing
            if user
              li.nav-item
                  a.nav-link(href='/logout')
                      span Logout
        span.navbar-text
            if user
              a.nav-link(href='/billing')
                  span #{user.username}
            else
              a.nav-link(href='/login')
                  span Login
